getwd()
setwd("/home/sima/git_projects/illumina-genotyping-pipeline/input_data/expr_array")

expr <- readRDS("annotated_expression_data.rds")
dim(expr)
colnames(expr)
head(expr[,1:5])
sum(duplicated(expr$SYMBOL))




out_tsv <- "expr_table.tsv"

write.table(
  expr,
  file = out_tsv,
  sep = "\t",
  quote = FALSE,
  row.names = FALSE
)